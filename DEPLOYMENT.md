# Deployment

Vercel-ready static application. Root entrypoint: `index.html`.

Recommended deployment branch: `agent/pegas-ltl-control-tower-v2`.

The app is fully client-side for demo purposes and stores operational state in LocalStorage. Production backend/API, auth, database, routing API and scanner hardware integrations are intentionally left as the next integration phase.
